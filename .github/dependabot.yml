version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]

  # No pip manifest is committed (test deps are installed ad hoc in CI), but
  # keep this so Dependabot picks up any future requirements file.
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
